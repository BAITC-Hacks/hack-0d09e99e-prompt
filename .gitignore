# partner 1C dumps — never commit
*.xlsx
*.xls
data/raw/

# derived partner data — same commercial content as the dumps, just computed.
# Never commit a workspace: the buyer uploads 1C Excel, then we compute.
backend/data/workspace.json
backend/data/uploads/
data/workspace.json
data/uploads/
**/iek.json

# secrets — hourly-push.sh runs `git add -A`, so anything not listed here
# gets committed and pushed within the hour. Add before creating the file.
.env
.env.*
*.pem
*.p8
*.key
service-account*.json
google-services.json
GoogleService-Info.plist

__pycache__/
.venv/
node_modules/
.next/
tsconfig.tsbuildinfo
.DS_Store
